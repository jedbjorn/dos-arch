# Credential-broker runtime deps — baked into the dos-broker image.
httpx==0.28.1
starlette==1.0.0
uvicorn==0.47.0
cryptography==48.0.0
pyotp==2.9.0          # RFC 6238 TOTP for the broker IdP (auth_store.py)
