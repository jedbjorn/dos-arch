[pytest]
# The app is launched with cwd=shell_core (ecosystem.config.cjs), so `api.main`
# and `shell_render` resolve from there — mirror that on the test path.
pythonpath = shell_core
testpaths = tests
addopts = -ra -q
